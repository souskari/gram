PRINT 2x + 5x^3 - 4 + x^2;
PRINT 3y^2 + 6x^2 - z^2;
PRINT x^2 + y^3;
PRINT 3b^3 + 2a^3 - c^3 + 5ab - 2ac + 4bc + a^2 + 10b^2 - c^2 + 7;
// $K = 2;
$J = -(x-x);
$A = x^$J;
PRINT $A;
// comment
 $B = 5z^2 + 2x;
PRINT $A;
PRINT $B;
PRINT $B; // 2nd call
()PRINT $B; // error
(PRINT($A)); // error
($A)PRINT($A); // error
PRINT $A($A); // okay
 $C = x;
PRINT x^($A); // error
PRINT x^$A; // error
PRINT (x+(x+(x+(x+(x+(x+(x+x)+x)+x)+x)+x)+x)+x);
PRINT PRINT; // error
 $D = x ++ x; // error
 $D = x + @; // error
 $D = x / 0; // error
 $D = x^-1; // error
 $D = x^-1^-1; // error
 $F = x^99999999999999999999999999999999; // error
 $F = x^1e20; // error
 $F = 2^9; // okay
 $F = (2^(2^3)); // okay
$A = 2;
PRINT $A;
$A = -(y+1)^2;
$B = (y-1)^2;
$A = $A + $B;
$B = $A - $B;
$A = $A - $B;
PRINT $A;
PRINT $B;
